services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: nexa
      POSTGRES_PASSWORD: nexa
      POSTGRES_DB: nexa
    ports:
      - "5432:5432"
    volumes:
      - nexa_postgres_data:/var/lib/postgresql/data

volumes:
  nexa_postgres_data:
