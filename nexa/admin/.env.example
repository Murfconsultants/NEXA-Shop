BACKEND_URL=http://localhost:4000

# Must match the backend's ADMIN_API_KEY. Deliberately NOT prefixed with
# NEXT_PUBLIC_ — this stays server-side (see lib/api.ts's "server-only" import).
ADMIN_API_KEY=change-me-admin
